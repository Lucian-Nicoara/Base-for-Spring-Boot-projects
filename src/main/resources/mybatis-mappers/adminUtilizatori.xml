<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace = "com.lucian.dgaspc.mapper.AdminUtilizatoriMapper">
	<select id = "getUtilizatori" resultType = "com.lucian.dgaspc.model.UserData">
		SELECT 
			u.id,
			u.username,
			u.nume,
			u.prenume,
			u.idCompartiment,
			c.nume as compartiment,
			u.registrationDate,
			u.stadiu
		FROM 
			user_data u
		LEFT JOIN compartimente c on c.id = u.idCompartiment
		ORDER BY u.nume ASC
	</select>
	
	<select id = "getUtilizatorByUsername" resultType = "com.lucian.dgaspc.model.UserData">
		SELECT 
			u.id,
			u.username,
			u.nume,
			u.prenume,
			u.idCompartiment,
			c.nume as compartiment,
			u.registrationDate,
			u.stadiu
		FROM 
			user_data u
		LEFT JOIN compartimente c on c.id = u.idCompartiment
		WHERE
			u.username = #{username}
	</select>
	<insert id="postUtilizator">
		INSERT INTO user_data VALUES(null, #{utilizator.username}, #{utilizator.password}, #{utilizator.nume}, #{utilizator.prenume}, #{utilizator.idCompartiment}, SYSDATE(), #{utilizator.stadiu})
	</insert>
	
	<update id="putUtilizator">
		UPDATE user_data 
		SET 
			username = #{utilizator.username},
			<if test="utilizator.password != null and utilizator.password != ''">
			password = #{utilizator.password},
			</if>
			nume = #{utilizator.nume},
			prenume = #{utilizator.prenume},
			idCompartiment = #{utilizator.idCompartiment},
			stadiu = #{utilizator.stadiu}
		WHERE
			id = #{utilizator.id}
	</update>
	
	<select id="getRolByUserIdAndRol" resultType="com.lucian.dgaspc.model.UserRole">
		SELECT * FROM user_roles WHERE userId = #{idUser} and authority = #{rol}
	</select>
	
	<insert id="postRolUtilizator">
		INSERT INTO user_roles VALUES(null, #{rol}, #{idUser})
	</insert>
	
	<delete id="deleteRolUtilizator">
		DELETE FROM user_roles WHERE userId = #{idUser} and authority = #{rol}
	</delete>
	
	<delete id="deleteAllByUserId">
		DELETE FROM user_roles WHERE userId = #{idUser}
	</delete>
</mapper>